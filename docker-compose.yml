version: '3.8'

services:
  pdf-rotator:
    build: .
    # Map a host port to container 8000. Use PDF_ROTATOR_PORT env var (default 8080)
    ports:
      - "${PDF_ROTATOR_PORT:-8080}:8000"
    volumes:
      - ./data:/data
    restart: unless-stopped
